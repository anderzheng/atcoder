/* C - Division by Two with Something */

/* binary bit move operation:
   if remove 1 bit at right, then add 1 bit at left(with the oppsite value)
   so, normal integers need 2*N times to get the orignal value(N is the bit nums).
   and the symmetric?? bits need only N/2 times
*/
